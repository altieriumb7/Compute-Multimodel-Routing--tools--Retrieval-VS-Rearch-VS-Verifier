[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "budgeted-tool-router"
version = "0.1.0"
description = "Budgeted multi-tool router (BM25 + Qdrant dense) MVP"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
